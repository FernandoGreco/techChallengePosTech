apiVersion: v1
kind: Service
metadata:
  name: oficina-api-service
  namespace: oficina
spec:
  selector:
    app: oficina-api

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

  type: ClusterIP